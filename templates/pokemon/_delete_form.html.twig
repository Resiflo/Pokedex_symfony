<form method="post" action="{{ path('pokemon_delete', {'id': pokemon.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer ce pokemon du pokedex ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pokemon.id) }}">
    <button class="btn">Supprimer</button>
</form>
